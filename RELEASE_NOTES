CoPlan — your schedule now works offline.
• If the server can't be reached, the app now shows the last data it synced instead of an error on your events.
• Your rotation, custody changes, one-off overrides, holidays and absences are all cached on your phone and used automatically while you're offline.
• Everything refreshes back to the latest as soon as the connection returns.
