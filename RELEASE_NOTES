CoPlan — set an end date for anything that repeats.
• Standing events, weekly custody handovers, weekday rules and recurring expenses can now take an optional end date, so they stop repeating after the day you choose.
• Leave it blank to repeat forever, just like before — set a date and the item simply stops the day after, across the calendar, dashboard and home-screen widgets.
